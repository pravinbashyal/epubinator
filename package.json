{
  "private": true,
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "build": "cd packages/epubinator && yarn build",
    "doctoc": "doctoc"
  },
  "dependencies": {},
  "devDependencies": {
    "doctoc": "^2.2.0"
  }
}
